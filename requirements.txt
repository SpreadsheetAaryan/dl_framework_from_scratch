# Core numerical computation
numpy>=1.24.0

# Optional plotting and visualization
matplotlib>=3.7.0

# Graph visualization for computational graphs
graphviz>=0.20.1

# For working with Jupyter notebooks (if using .ipynb examples)
jupyterlab>=4.0.0

# If comparing results with PyTorch (optional)
torch>=2.0.0

# For type checking, if included in the dev pipeline
typing-extensions>=4.5.0

# Optional: pipeline support (e.g., experiment tracking, config management)
tqdm>=4.65.0
scikit-learn>=1.3.0  # For train/test utilities, metrics if needed

# Optional: saving/loading models and plots
joblib>=1.3.0
